import { AvailabilityManager } from "@/components/dashboard/availability-manager";

export const metadata = { title: "Disponibilità — Dashboard" };

export default function AvailabilityPage() {
  return <AvailabilityManager />;
}
