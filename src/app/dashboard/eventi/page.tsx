import { EventsManager } from "@/components/dashboard/events-manager";

export const metadata = { title: "Eventi — Dashboard" };

export default function DashboardEventsPage() {
  return <EventsManager />;
}
