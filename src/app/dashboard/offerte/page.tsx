import { OffersManager } from "@/components/dashboard/offers-manager";

export const metadata = { title: "Offerte — Dashboard" };

export default function DashboardOffersPage() {
  return <OffersManager />;
}
